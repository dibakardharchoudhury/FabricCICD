# ============================================================================
# fabric-cicd parameterization for PL_Refresh_Master and friends.
# ============================================================================
# THIS FILE IS AUTO-GENERATED by NB_05_Deploy at deploy time (it discovers every
# item from the repo folders, resolves each Dev GUID by name, and overwrites the
# rules below). You normally do NOT edit it by hand.
#
# It exists in Git only as the FALLBACK used when NB_05 runs with
# generate_parameter_yml = False. In that mode, fill in your real Dev GUIDs
# below; otherwise leave it as-is and let NB_05 regenerate it.
#
# Dynamic tokens (resolved against the TARGET workspace as it publishes):
#   $workspace.$id                       -> the workspace being published into
#   $items.Notebook.<DisplayName>.$id    -> that notebook's id in the target ws
#   $items.Dataflow.<DisplayName>.$id    -> that dataflow's id in the target ws
#   $items.SemanticModel.<Name>.$id      -> that semantic model's id in the target ws
#   $items.Lakehouse.<DisplayName>.$id   -> that lakehouse's id in the target ws
# ----------------------------------------------------------------------------

find_replace: []
